---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  parentRefs:
    - name: homelab
      namespace: gateway
  hostnames:
    - grafana.motherbox.local
  rules:
    - backendRefs:
        - name: grafana
          port: 80

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd
  namespace: core-argocd
spec:
  parentRefs:
    - name: homelab
      namespace: gateway
  hostnames:
    - argocd.motherbox.local
  rules:
    - backendRefs:
        - name: argocd-server
          port: 80
